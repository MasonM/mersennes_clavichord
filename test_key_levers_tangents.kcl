import "key_levers.kcl" as keyLevers
import "tangents.kcl" as tangents
import * from "utils.kcl"
import * from "params.kcl"

// keyLeverStartY
cube([keywellLength, 5, 5])
  |> translate(x = toolboxLength, y = keywellWidth, z = 200)

// keyLeverAllNarrowY
cube([keywellLength, 5, 5])
  |> translate(x = toolboxLength, y = keywellWidth + 24 + 12, z = 200)

// keyLeverBendY
cube([keywellLength, 5, 5])
  |> translate(x = toolboxLength, y = keywellWidth + 24 + 12 + 20, z = 200)
